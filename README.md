# LibSTF
A .NET library for modding Sonic the Fighters.

Currently only supports the PS3 release's romset, splitting the ROMS for *MAME*/*Model 2 Emulator* will have to be done manually.

## Currently supports:
- Model injection
